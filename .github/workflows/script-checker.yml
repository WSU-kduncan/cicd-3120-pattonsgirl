name: play-with-actions

on: [push]

jobs:
  run-bats-on-script:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pre-checkout" && ls -lah
      - uses: actions/checkout@v3
      - run: echo "post-checkout" && ls -lah
